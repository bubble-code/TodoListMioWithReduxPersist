import React, {useContext, FC} from 'react'
// import {TodoContext} from '../../App'
import {Button} from 'antd' 

export const Complete:FC = ()=>{
    // const [, dispaatchTodos] = useContext(TodoContext);

    return(
        <a href="#complete" onClick={()=>{}}/>
    );
}